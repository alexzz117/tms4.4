<?xml version="1.0" encoding="UTF-8"?>

<!-- 根节点<configuration>包含的属性： scan: 当此属性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true。 
	scanPeriod: 设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。 
	debug: 当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 -->
<configuration>
	
	<!-- 日志文件名 -->
	<substitutionProperty name="log.name" value="tms-master" />
	<!-- 测试环境,使用的日志路径 -->
	<substitutionProperty name="log.base" value="./logs" />
	<!-- 投产时,使用的日志路径 -->
	<!-- <substitutionProperty name="log.base" value="/opt/log/app-tms-web-zhcx" /> -->

	<!-- ch.qos.logback.core.ConsoleAppender 控制台输出 -->
	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%-40.40logger:%-3.3line] [%.20thread] - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- 按照每天生成日志文件 -->
	<!-- <appender name="logfile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<File>${log.base}/${log.name}.log</File>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			日志文件输出的文件名
			<FileNamePattern>${log.base}/${log.name}-%d{yyyy-MM-dd}.log.%i</FileNamePattern>
			日志文件保留天数
			<MaxHistory>7</MaxHistory>
			<TimeBasedFileNamingAndTriggeringPolicy
				class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				日志文件最大的大小
				<MaxFileSize>100MB</MaxFileSize>
			</TimeBasedFileNamingAndTriggeringPolicy>
		</rollingPolicy>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符
			<pattern>[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%-40.40logger:%-3.3line] [%.20thread] - %msg%n</pattern>
		</encoder>
	</appender> -->

	<!-- 日志级别 -->
	<root>
		<level value="info" />
		<!-- <appender-ref ref="console" /> -->
		<!-- <appender-ref ref="logfile" /> -->
	</root>
</configuration> 